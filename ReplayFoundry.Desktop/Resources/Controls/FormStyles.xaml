<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="{x:Static CalendarItem.DayTitleTemplateResourceKey}">
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="9"
            FontWeight="SemiBold"
            Foreground="{DynamicResource Brush.TextMuted}"
            Text="{Binding}" />
    </DataTemplate>
    <Style x:Key="Control.FormTextBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource Control.ThemedTextBox}" />
    <Style x:Key="Control.FormComboBox" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource Control.ThemedComboBox}" />
    <Style x:Key="Control.FormCheckBox" TargetType="{x:Type CheckBox}" BasedOn="{StaticResource Control.ThemedCheckBox}" />
    <Style x:Key="Control.ThemedCalendarDayButton" TargetType="{x:Type CalendarDayButton}" BasedOn="{StaticResource Control.KeyboardFocus}">
        <Setter Property="MinWidth" Value="30" />
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="Margin" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                    <Border
                        x:Name="DaySurface"
                        Background="{TemplateBinding Background}"
                        BorderBrush="Transparent"
                        BorderThickness="1"
                        CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsInactive" Value="True"><Setter Property="Opacity" Value="0.42" /></Trigger>
                        <Trigger Property="IsToday" Value="True"><Setter TargetName="DaySurface" Property="BorderBrush" Value="{DynamicResource Brush.StatusInfo}" /></Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="DaySurface" Property="Background" Value="{DynamicResource Brush.InteractiveHover}" /></Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter
                                TargetName="DaySurface"
                                Property="Background"
                                Value="{DynamicResource Brush.InteractiveSelected}" />
                            <Setter
                                TargetName="DaySurface"
                                Property="BorderBrush"
                                Value="{DynamicResource Brush.StatusInfo}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="DaySurface" Property="BorderBrush" Value="{DynamicResource Brush.BorderFocus}" />
                            <Setter TargetName="DaySurface" Property="BorderThickness" Value="2" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.32" /></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Control.ThemedCalendarButton" TargetType="{x:Type CalendarButton}" BasedOn="{StaticResource Control.KeyboardFocus}">
        <Setter Property="MinWidth" Value="58" />
        <Setter Property="MinHeight" Value="42" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarButton}">
                    <Border x:Name="MonthSurface" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="1" CornerRadius="7">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsInactive" Value="True"><Setter Property="Opacity" Value="0.4" /></Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="MonthSurface" Property="Background" Value="{DynamicResource Brush.InteractiveHover}" /></Trigger>
                        <Trigger Property="IsFocused" Value="True"><Setter TargetName="MonthSurface" Property="BorderBrush" Value="{DynamicResource Brush.BorderFocus}" /></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Control.ThemedCalendarItem" TargetType="{x:Type CalendarItem}">
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource Brush.SurfaceElevated}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarItem}">
                    <Grid x:Name="PART_Root" Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid Margin="4,3,4,7">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="38" /><ColumnDefinition Width="*" /><ColumnDefinition Width="38" /></Grid.ColumnDefinitions>
                            <Button
                                x:Name="PART_PreviousButton"
                                AutomationProperties.Name="Previous month"
                                Style="{DynamicResource Control.IconButton}"
                                ToolTip="Previous month">
                                <Path Width="13" Height="13" Data="M 10,2 L 4,7 L 10,12" Stroke="{DynamicResource Brush.TextPrimary}" StrokeThickness="1.5" Stretch="Uniform" />
                            </Button>
                            <Button
                                x:Name="PART_HeaderButton"
                                Grid.Column="1"
                                AutomationProperties.Name="Choose month or year"
                                Background="Transparent"
                                BorderThickness="0"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource Brush.TextPrimary}" />
                            <Button
                                x:Name="PART_NextButton"
                                Grid.Column="2"
                                AutomationProperties.Name="Next month"
                                Style="{DynamicResource Control.IconButton}"
                                ToolTip="Next month">
                                <Path Width="13" Height="13" Data="M 4,2 L 10,7 L 4,12" Stroke="{DynamicResource Brush.TextPrimary}" StrokeThickness="1.5" Stretch="Uniform" />
                            </Button>
                        </Grid>
                        <Grid x:Name="PART_MonthView" Grid.Row="1" Margin="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                        </Grid>
                        <Grid x:Name="PART_YearView" Grid.Row="1" Margin="4" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Control.ThemedCalendar" TargetType="{x:Type Calendar}" BasedOn="{StaticResource Control.KeyboardFocus}">
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource Brush.SurfaceElevated}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CalendarButtonStyle" Value="{StaticResource Control.ThemedCalendarButton}" />
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource Control.ThemedCalendarDayButton}" />
        <Setter Property="CalendarItemStyle" Value="{StaticResource Control.ThemedCalendarItem}" />
    </Style>
    <Style x:Key="Control.ThemedDatePickerTextBox" TargetType="{x:Type DatePickerTextBox}" BasedOn="{StaticResource Control.KeyboardFocus}">
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                    <Grid>
                        <ScrollViewer
                            x:Name="PART_ContentHost"
                            Margin="{TemplateBinding Padding}"
                            VerticalAlignment="Center" />
                        <ContentControl
                            x:Name="PART_Watermark"
                            Margin="10,0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource Brush.TextMuted}"
                            Focusable="False"
                            IsHitTestVisible="False"
                            Opacity="0.7"
                            Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Control.DatePickerCalendarButton" TargetType="{x:Type Button}">
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.72" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.48" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Control.ThemedDatePicker" TargetType="{x:Type DatePicker}" BasedOn="{StaticResource Control.KeyboardFocus}">
        <Setter Property="MinHeight" Value="{DynamicResource Dimension.ControlHeight}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource Brush.SurfaceInset}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderSubtle}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CalendarStyle" Value="{StaticResource Control.ThemedCalendar}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePicker}">
                    <Grid x:Name="PART_Root">
                        <Border x:Name="DateSurface" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="40" /></Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0" Style="{StaticResource Control.ThemedDatePickerTextBox}" />
                                <Border
                                    x:Name="CalendarButtonSurface"
                                    Grid.Column="1"
                                    Background="{DynamicResource Brush.SurfacePanel}"
                                    BorderBrush="{DynamicResource Brush.BorderSubtle}"
                                    BorderThickness="1,0,0,0"
                                    CornerRadius="0,7,7,0">
                                    <Button
                                        x:Name="PART_Button"
                                        Focusable="False"
                                        Style="{StaticResource Control.DatePickerCalendarButton}"
                                        ToolTip="Open calendar">
                                        <Path Width="17" Height="17" Data="M 2,4 L 14,4 L 14,14 L 2,14 Z M 2,7 L 14,7 M 5,2 L 5,5 M 11,2 L 11,5" Stroke="{DynamicResource Brush.StatusInfo}" StrokeThickness="1.5" Stretch="Uniform" />
                                    </Button>
                                </Border>
                            </Grid>
                        </Border>
                        <Popup
                            x:Name="PART_Popup"
                            AllowsTransparency="True"
                            Focusable="False"
                            IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                            Placement="Bottom"
                            PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                            PopupAnimation="Fade"
                            StaysOpen="False"
                            VerticalOffset="6">
                            <Border Background="{DynamicResource Brush.SurfaceElevated}" BorderBrush="{DynamicResource Brush.BorderStrong}" BorderThickness="1" CornerRadius="10" Padding="6">
                                <Calendar x:Name="PART_Calendar" Style="{StaticResource Control.ThemedCalendar}" />
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="DateSurface" Property="BorderBrush" Value="{DynamicResource Brush.BorderStrong}" /></Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="DateSurface" Property="BorderBrush" Value="{DynamicResource Brush.BorderFocus}" /></Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True"><Setter TargetName="DateSurface" Property="BorderBrush" Value="{DynamicResource Brush.StatusInfo}" /></Trigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="CalendarButtonSurface" Property="Background" Value="{DynamicResource Brush.InteractiveHover}" /></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="DateSurface" Property="Opacity" Value="0.48" /></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="Control.FormDatePicker" TargetType="{x:Type DatePicker}" BasedOn="{StaticResource Control.ThemedDatePicker}" />
    <Style x:Key="Text.FormLabel" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Margin" Value="0,0,0,4" />
    </Style>
</ResourceDictionary>
