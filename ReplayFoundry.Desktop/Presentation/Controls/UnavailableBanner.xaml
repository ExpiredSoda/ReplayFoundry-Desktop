<UserControl
    x:Class="ReplayFoundry.Desktop.Presentation.Controls.UnavailableBanner"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    AutomationProperties.Name="Unavailable capability">
    <Border Padding="12,10" Background="{DynamicResource Brush.SurfaceInset}" BorderBrush="{DynamicResource Brush.StatusWarning}" BorderThickness="1" CornerRadius="10">
        <StackPanel>
            <TextBlock Foreground="{DynamicResource Brush.StatusWarning}" FontSize="12" FontWeight="SemiBold" Text="{Binding Title, ElementName=Root}" TextWrapping="Wrap" />
            <TextBlock Margin="0,4,0,0" Style="{DynamicResource Text.Body}" Text="{Binding Description, ElementName=Root}" />
        </StackPanel>
    </Border>
</UserControl>
